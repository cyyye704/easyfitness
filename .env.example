# Set this to true to expose the natural-language parsing endpoint.
AI_FEATURE_ENABLED=true

# Server-only secret. Never use a VITE_ prefix and never commit a real key.
DEEPSEEK_API_KEY=

# Optional. The server defaults to deepseek-v4-flash.
DEEPSEEK_MODEL=deepseek-v4-flash
